_min_copier_version: "8.0"

project_name:
  type: str
  help: "project name?"

python_path:
  type: str
  help: "python path?"

_tasks:
  - "cd {{ _copier_dst_path }} && {{ python_path }} -m venv .venv"
  - "cd {{ _copier_dst_path }} && .venv\\Scripts\\pip.exe install -r requirements.txt"
_min_copier_version: "8.0"

project_name:
  type: str
  help: "project name?"

python_path:
  type: str
  help: "python path?"

_tasks:
  - "{{ python_path }} -m venv .venv"
  - ".venv\\Scripts\\pip.exe install -r requirements.txt"
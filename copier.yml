_min_copier_version: "8.0"

project_name:
  type: str
  help: "project name?"

python_path:
  type: str
  help: "python path?"

_tasks:
  - "{{ python_path }} -m {{ _copier_dst_path }} venv .venv"
  - "{{ _copier_dst_path }}\\.venv\\Scripts\\pip.exe install -r requirements.txt"